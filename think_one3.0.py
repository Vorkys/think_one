#for x in range(9):
#    exec("string" + str(x) + " = 'Hello' + str(x)")

#print(string6)

class Animals:
    def __init__(self, name, color, leg_num, tail, size, spec):
        self.name = name
        self.color = color
        self.leg_num = leg_num
        self.tail = tail
        self.size = size
        self.spec = spec
